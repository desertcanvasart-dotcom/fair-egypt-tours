{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "mkdir -p data && npx prisma migrate deploy && npm run db:seed && npm run build"
  },
  "deploy": {
    "startCommand": "mkdir -p data && npx prisma migrate deploy && npm run db:seed && npm run start -- -H 0.0.0.0 -p ${PORT:-3000}",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
